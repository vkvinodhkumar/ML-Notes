# Data

The planned notebook uses scikit-learn's built-in breast-cancer classification dataset and a deterministic synthetic moons dataset. Both require no network download and support reproducible binary-classification, margin, staged-performance, and nonlinear-boundary diagnostics.
