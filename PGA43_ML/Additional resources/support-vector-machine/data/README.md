# Data

The planned executed notebook uses scikit-learn's built-in breast-cancer classification dataset and a deterministic synthetic moons dataset. No network download is required. The breast-cancer data supports realistic binary evaluation, while the moons data makes linear versus RBF decision geometry visible.
