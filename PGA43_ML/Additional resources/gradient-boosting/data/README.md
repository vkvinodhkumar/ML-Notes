# Data

The notebook uses scikit-learn's built-in diabetes regression dataset. No network download is required. The dataset is deterministic, small enough for teaching, and suitable for regression diagnostics.

For deployment-oriented practice, replace it with a domain dataset while preserving the same split, preprocessing, validation, and reproducibility discipline.