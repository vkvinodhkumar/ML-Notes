# Data

The notebook uses scikit-learn's built-in breast-cancer classification dataset. It is deterministic, numeric, moderate-dimensional, and requires no network download.

The dataset supports variance analysis, reconstruction diagnostics, two-dimensional visualization, and leakage-safe downstream classification.
