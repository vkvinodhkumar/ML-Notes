# Data

The notebook uses a built-in scikit-learn binary-classification dataset, so no network access is required. It applies a stratified split with fixed random seed 42 and keeps the final test partition untouched until model selection is complete.
